# Moved to the canonical README

This file was folded into the main [README](../../README.md), which now documents the project layout (`src/client`, `src/server`, `src/shared`, `ReplicatedStorage/Remotes`), build/run steps for **`Animal Crossing CE.rbxlx`**, and the playtest workflow. Check that file (plus `docs/DOCUMENTATION_PORTAL.md`) for current guidance so setup instructions stay in sync.
