name: Luau CI

on:
  push:
    branches:
      - master
      - main
  pull_request:

jobs:
  static-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip curl

      - name: Install Aftman
        run: |
          curl -fsSL https://github.com/LPGhatguy/aftman/releases/latest/download/aftman-x86_64-unknown-linux-gnu.zip -o aftman.zip
          unzip aftman.zip -d aftman-bin
          install aftman-bin/aftman $HOME/.local/bin/aftman
          echo "$HOME/.aftman/bin" >> "$GITHUB_PATH"

      - name: Install toolchain
        run: |
          $HOME/.local/bin/aftman install

      - name: Run static checks
        run: bash tools/run_static_checks.sh
