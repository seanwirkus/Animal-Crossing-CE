# Island Generation GUI (Deprecated)

The Island Generation GUI has been removed. Island resources (trees/rocks) now auto-populate and respawn through `ResourceSpawner` and player island generation, without any player-facing generation UI.

If you need manual testing or debugging hooks, use the server-side quick test utilities instead.
